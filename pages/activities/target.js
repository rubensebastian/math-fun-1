import Menu from '../../components/menu';
import Target from '../../components/target';

export default function Pairing() {
    return(
        <div>
            <nav>
                <Menu />
            </nav>
            <main>
                <Target />
            </main>
        </div>
    );
}