import Water from '../../components/water';
import Menu from '../../components/menu';

export default function Home() {
    return (
        <div>
            <Menu />
            <main>
                <Water />
            </main>
        </div>
    );
}