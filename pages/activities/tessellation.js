import Menu from '../../components/menu';
import Tessellation from '../../components/tessellation';

export default function Tessellate() {
    return(
        <div>
            <nav>
                <Menu />
            </nav>
            <main>
                <Tessellation />
            </main>
        </div>
    );
}